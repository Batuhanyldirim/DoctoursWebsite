/* document.addEventListener('DOMContentLoaded', () => {
    const servicesText1 = document.querySelector('.services-text1');
    const ivfServiceImage1 = document.querySelector('.ivfServiceImg1');

    ivfServiceImage1.addEventListener('mouseover', () => {
        servicesText1.style.display = 'block';

        servicesText2.style.display = 'none';//they are delete code
        servicesText4.style.display = 'none';//they are delete code
        servicesText5.style.display = 'none';//they are delete code
        ivfServiceImage2.addEventListener('mouseout', hideServicesText2);//they are delete code
        ivfServiceImage4.addEventListener('mouseout', hideServicesText4);//they are delete code
        ivfServiceImage5.addEventListener('mouseout', hideServicesText5);//they are delete code
    });

    ivfServiceImage1.addEventListener('click', () => {
        servicesText1.style.display = 'block';
        ivfServiceImage1.removeEventListener('mouseout', hideServicesText1);

        servicesText2.style.display = 'none';//they are delete code
        servicesText4.style.display = 'none';//they are delete code
        servicesText5.style.display = 'none';//they are delete code
        ivfServiceImage2.addEventListener('mouseout', hideServicesText2);//they are delete code
        ivfServiceImage4.addEventListener('mouseout', hideServicesText4);//they are delete code
        ivfServiceImage5.addEventListener('mouseout', hideServicesText5);//they are delete code
    });

    const hideServicesText1 = () => {
        servicesText1.style.display = 'none';
    };

    ivfServiceImage1.addEventListener('mouseout', hideServicesText1);





    const servicesText2 = document.querySelector('.services-text2');
    const ivfServiceImage2 = document.querySelector('.ivfServiceImg2');

    ivfServiceImage2.addEventListener('mouseover', () => {
        servicesText2.style.display = 'block';

        servicesText1.style.display = 'none';//they are delete code
        servicesText4.style.display = 'none';//they are delete code
        servicesText5.style.display = 'none';//they are delete code
        ivfServiceImage1.addEventListener('mouseout', hideServicesText1);//they are delete code
        ivfServiceImage4.addEventListener('mouseout', hideServicesText4);//they are delete code
        ivfServiceImage5.addEventListener('mouseout', hideServicesText5);//they are delete code
    });

    ivfServiceImage2.addEventListener('click', () => {
        servicesText2.style.display = 'block';
        ivfServiceImage2.removeEventListener('mouseout', hideServicesText2);

        servicesText1.style.display = 'none';//they are delete code
        servicesText4.style.display = 'none';//they are delete code
        servicesText5.style.display = 'none';//they are delete code
        ivfServiceImage1.addEventListener('mouseout', hideServicesText1);//they are delete code
        ivfServiceImage4.addEventListener('mouseout', hideServicesText4);//they are delete code
        ivfServiceImage5.addEventListener('mouseout', hideServicesText5);//they are delete code
    });

    const hideServicesText2 = () => {
        servicesText2.style.display = 'none';
    };

    ivfServiceImage2.addEventListener('mouseout', hideServicesText2);







    const servicesText4 = document.querySelector('.services-text4');
    const ivfServiceImage4 = document.querySelector('.ivfServiceImg4');

    ivfServiceImage4.addEventListener('mouseover', () => {
        servicesText4.style.display = 'block';

        servicesText1.style.display = 'none';//they are delete code
        servicesText2.style.display = 'none';//they are delete code
        servicesText5.style.display = 'none';//they are delete code
        ivfServiceImage1.addEventListener('mouseout', hideServicesText1);//they are delete code
        ivfServiceImage2.addEventListener('mouseout', hideServicesText2);//they are delete code
        ivfServiceImage5.addEventListener('mouseout', hideServicesText5);//they are delete code


    });

    ivfServiceImage4.addEventListener('click', () => {
        servicesText4.style.display = 'block';
        ivfServiceImage4.removeEventListener('mouseout', hideServicesText4);

        servicesText1.style.display = 'none';//they are delete code
        servicesText2.style.display = 'none';//they are delete code
        servicesText5.style.display = 'none';//they are delete code
        ivfServiceImage1.addEventListener('mouseout', hideServicesText1);//they are delete code
        ivfServiceImage2.addEventListener('mouseout', hideServicesText2);//they are delete code
        ivfServiceImage5.addEventListener('mouseout', hideServicesText5);//they are delete code
    });

    const hideServicesText4 = () => {
        servicesText4.style.display = 'none';
    };

    ivfServiceImage4.addEventListener('mouseout', hideServicesText4);




    const servicesText5 = document.querySelector('.services-text5');
    const ivfServiceImage5 = document.querySelector('.ivfServiceImg5');

    ivfServiceImage5.addEventListener('mouseover', () => {
        servicesText5.style.display = 'block';

        servicesText1.style.display = 'none';//they are delete code
        servicesText2.style.display = 'none';//they are delete code
        servicesText4.style.display = 'none';//they are delete code
        ivfServiceImage1.addEventListener('mouseout', hideServicesText1);//they are delete code
        ivfServiceImage2.addEventListener('mouseout', hideServicesText2);//they are delete code
        ivfServiceImage4.addEventListener('mouseout', hideServicesText4);//they are delete code


    });

    ivfServiceImage5.addEventListener('click', () => {
        servicesText5.style.display = 'block';
        ivfServiceImage5.removeEventListener('mouseout', hideServicesText5);

        servicesText1.style.display = 'none';//they are delete code
        servicesText2.style.display = 'none';//they are delete code
        servicesText4.style.display = 'none';//they are delete code
        ivfServiceImage1.addEventListener('mouseout', hideServicesText1);//they are delete code
        ivfServiceImage2.addEventListener('mouseout', hideServicesText2);//they are delete code
        ivfServiceImage4.addEventListener('mouseout', hideServicesText4);//they are delete code
    });

    const hideServicesText5 = () => {
        servicesText5.style.display = 'none';
    };

    ivfServiceImage5.addEventListener('mouseout', hideServicesText5);


});


 */